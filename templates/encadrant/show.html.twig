{% extends 'baselayout.html.twig' %}


{% block title %}Encadrant{% endblock %}

{% block body %}
    <h1>Encadrant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ encadrant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ encadrant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ encadrant.prenom }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ encadrant.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ encadrant.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('encadrant_index') }}">back to list</a>

    <a href="{{ path('encadrant_edit', {'id': encadrant.id}) }}">edit</a>

    {{ include('encadrant/_delete_form.html.twig') }}
{% endblock %}
